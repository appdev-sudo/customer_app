export type LongevityPackage = {
  id: string;
  title: string;
  duration: string;
  idealFor?: string;
  bullets: string[];
  /** Show "Founding 100 Access — Benefits" orange button (Image 2, Longevity Protocol only) */
  hasFounding100?: boolean;
};

